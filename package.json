{
  "name": "starframe",
  "private": true,
  "version": "0.1.0-dev.1",
  "description": "Desktop mod manager for Sanctuary: Shattered Sun",
  "license": "AGPL-3.0-only",
  "type": "module",
  "engines": {
    "node": ">=24.19.0 <25"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "format": "prettier --write src package.json tsconfig.json vite.config.ts svelte.config.js eslint.config.js index.html .prettierrc.json .github/workflows .github/dependabot.yml",
    "format:check": "prettier --check src package.json tsconfig.json vite.config.ts svelte.config.js eslint.config.js index.html .prettierrc.json .github/workflows .github/dependabot.yml",
    "lint": "eslint . --max-warnings 0",
    "check:types": "svelte-check --tsconfig ./tsconfig.json --fail-on-warnings",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm run format:check && npm run lint && npm run check:types && npm test && npm run build",
    "check:rust": "cargo fmt --manifest-path src-tauri/Cargo.toml --check && cargo clippy --manifest-path src-tauri/Cargo.toml --locked --all-targets -- -D warnings && cargo test --manifest-path src-tauri/Cargo.toml --locked"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@sveltejs/vite-plugin-svelte": "^7.3.0",
    "@tauri-apps/cli": "^2.11.4",
    "eslint": "^10.10.0",
    "eslint-plugin-svelte": "^3.23.0",
    "globals": "^17.12.0",
    "prettier": "^3.9.6",
    "prettier-plugin-svelte": "^4.1.1",
    "svelte": "^5.57.0",
    "svelte-check": "^4.7.6",
    "typescript": "~6.0.3",
    "typescript-eslint": "^8.69.0",
    "vite": "^8.2.2",
    "vitest": "^5.0.0"
  }
}
